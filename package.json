{
  "name": "guardrail-api",
  "version": "0.1.0",
  "private": true,
  "description": "Toy guardrails gateway \u2014 classifies prompts safe/unsafe and applies policy before passing to a downstream model. Demo repo for the 'Building Software with Securely Autonomous Agent Teams' masterclass.",
  "main": "dist/src/index.js",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/src/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.2",
    "vitest": "^4.1.11"
  }
}
